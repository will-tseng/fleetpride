# NPM Configuration for UI project

# Version and Git settings
tag-version-prefix=v
message="%s"
git-tag-version=true
commit-hooks=true

# Registry settings
registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate
fund=false

# Install settings
save-exact=true
engine-strict=true
